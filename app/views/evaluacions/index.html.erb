<div class="align_center">
  <h1>Evaluaciones de <%= @course.nombre %></h1>
</div>

<div class="actions">
  <%= link_to "Nueva", new_course_evaluacion_path(@course), :class => "crear_nuevo btn-small" %>
</div>

<%= pluralize @evaluaciones.count, "evaluación", "evaluaciones" %>

<% @evaluaciones.each do |evaluacion| %>
  <%= render 'evaluacion_resume', evaluacion: evaluacion %>
<% end %>
